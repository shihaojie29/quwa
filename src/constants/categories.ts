import type { ChecklistCategory } from "../types/checklist";
import type { ExpenseCategory } from "../types/expense";

export const EXPENSE_CATEGORIES: readonly ExpenseCategory[] = ["交通", "住宿", "餐饮", "景点", "其他"];

export const CHECKLIST_CATEGORIES: readonly ChecklistCategory[] = ["证件证卡", "衣物", "电子", "其他"];
